datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
} 

model UserAPILimit {
  id String @id @default(cuid())
  userId String @unique
  count Int @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model UserSubcription {
  id String @id @default(cuid())
  userId String @unique // comes from clerk
  stripeCustomerId String? @unique @map(name:"stripe_customer_id") //stripe_customer_id this case is use by stripe
  stripeSubscriptionId String? @unique @map(name:"stripe_subscription_id")
  stripePriceId String? @map(name:"stripe_price_id")
  stripeCurrentPeriodEnd DateTime? @map(name:"stripe_current_period_end") 
}


// Every time when we modify schema we have o perform these operations
// 1. npx prisma generate (to add it into node_modules)
// 2. npx prisma db push (to push at planteScale)